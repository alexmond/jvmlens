# Brainstorm panel — log

What the panel has learned about how work goes in this repo. Bias future rosters
with this; graduate stable findings into `CLAUDE.md` (`## Panel roles`) and prune.

## 2026-06-27 — "what is next" (open ideation / roadmap direction)
- Proposed: product-strategist (director), java-dev-advocate, ai-tooling, observability-moat-skeptic, oss-release. User added: none. User removed: none — approved as-is.
- Style: **swarm → director-led** — fit well; converged in **1 round** (pushback roles' "tempting-but-wrong" notes resolved the avoid-list, so no 2nd round needed).
- Recurring: **4/5 roles independently ranked Maven Central release the keystone bottleneck** ("validated but uninstallable-as-a-dependency"). **2 roles (strategist + moat-skeptic) independently flagged MCP-over-HTTP sidecar as the trap** (APM camel's nose). Also rejected by ≥1 role each: auto-fix/apply-patch mode, a CLI Docker image, a web dashboard / more profiling dimensions.
- Note: thesis discipline is unusually strong here — **"not an APM" is load-bearing** and every role self-policed scope drift. For roadmap/strategy targets on this repo, always seat the **moat-skeptic** (keeps the avoid-list honest) and the **ai-tooling** role (the consumer is a coding agent — easy to under-weight). The director-led converge worked because the moat-vs-distribution tension is real and needs one owner to call it.
