# Datasource (url/username/password) is supplied by the integration test from the
# Testcontainers Postgres instance. Random HTTP port so parallel runs don't collide.
server.port=0
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.main.banner-mode=off
